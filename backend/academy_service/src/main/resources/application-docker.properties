# Docker/deploy profile overrides.
# SECURITY: no API-docs/Swagger surface in deployed profiles (prevents API-inventory disclosure).
# Base application.properties enables springdoc for local dev; the deploy runs this profile.
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
